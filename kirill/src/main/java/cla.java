import java.io.IOException;


public class cla {
    public static void main(String[] args) throws IOException {

//new Jfx().start(args);
       /*ArrayList gogo = new ArrayList();
        gogo.add(Office.getAdmins());

        //.inctaneOf(Manager);
String ... args // skolko hozhesh znacheniy


       /* ArrayList<String> strings =new ArrayList <String>();
        strings.add("blala");

        File f = new File("examples\\test.txt");
        FileWriter writer = new FileWriter(f, true); //dopisivaet v konec esli tru
        writer.write("Privet"+'\n');
        writer.flush();
        writer.close();

        PrintWriter printWriter = new PrintWriter(f);


        LocalDate date = LocalDate.of(1985,10,14);  //.now();
        date.getDayOfMonth(); //plusdays

        int a=4;
        int b=2;

        System.out.println(a*b);
        System.out.println(a/b);
        System.out.println(a-b);
        System.out.println(a%b);

        int a=1;
        int b=2;
        int c=3;
        int[] g={a,b,c}; //массив любого типа данных
        int[] d={1,3,7}; //первый способ

        int[] array = new int[5];//второй способ задать
        array[0] = -12;
        array[1] = 125;
        array[2] = 53;
        array[3] = 244;
        array[4] = 11;

      for(int i=0;i<array.length;i++){            //pravilniy poryadok

        System.out.println(array[i]);}

        for(int i=(array.length-1);i>=0;i--){       //obratniy poryadok

        System.out.println(array[i]);}

        for(int i=(array.length-1)/2;i>=0;i--){     //obratniy poryadok polovina

        System.out.println(array[i]);}

        for(int i=0;i<array.length/2;i++){          //pravilniy poryadok polovina

        System.out.println(array[i]);}
        for (int i = 0; i < array.length; i++) {

            System.out.print(array[i] + ", ");
        }
        System.out.println();
        int min = array[0];
        int max = array[0];
        for (int i = 1; i < array.length; i++) {

            if (array[i] < min) {
                min = array[i];
            }
            if (array[i] > max) {
                max = array[i];
            }
        }
          System.out.println();
        System.out.println("max=" + max + " min=" + min);
        int y = max;
        for (int i = 0; i < array.length; i++) {
            if (max == array[i])                             //сделать без цикла
                array[i] = min;
            else if (min == array[i])
                array[i] = y;

        }
        for (int i = 0; i < array.length; i++) {
            System.out.print(array[i] + ", ");
        }*/



    }
}
        /*psvm -  main ;  sout
         if (true && ++i ==0){ sout(i); =0
         if (true & ++i ==0){ sout(i); =1
         if (!(2==2) = инверсия if (!true) =false
         fori +tab (счетчик условие шаг)

         i=0
         String str="Test"
         switch(i){
         case 0:
         System.out.println("case zero"); break;
         case 1:
         System.out.println("case one"); break;
         case "Test" :
         System.out.println
         default:
         System.out.println("no cases detected");*/







